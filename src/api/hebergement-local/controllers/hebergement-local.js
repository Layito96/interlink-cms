'use strict';

/**
 * hebergement-local controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::hebergement-local.hebergement-local');
